version: "3.9"

services:
  redis:
    image: redis:latest
    command: redis-server --requirepass ${REDIS_PASSWORD}
    networks:
      - internal_net
    restart: unless-stopped

  postgres:
    image: postgres:latest
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - pgdata:/var/lib/postgresql/data
    networks:
      - internal_net
    restart: unless-stopped

  waha:
    image: devlikeapro/waha:latest
    environment:
      WHATSAPP_HOOK_URL: https://n8n.meudevlab.com.br/webhook/webhook
      WAHA_API_KEY: ${WAHA_API_KEY}
    volumes:
      - waha_sessions:/app/.sessions
      - waha_media:/app/.media
    networks:
      - internal_net
    depends_on:
      - redis
      - postgres
    restart: unless-stopped

  n8n:
    image: n8nio/n8n:latest
    environment:
      WEBHOOK_URL: https://n8n.meudevlab.com.br
      N8N_HOST: n8n.meudevlab.com.br
      GENERIC_TIMEZONE: America/Sao_Paulo
    volumes:
      - n8n_data:/home/node/.n8n
    networks:
      - internal_net
    restart: unless-stopped

  proxy:
    image: jc21/nginx-proxy-manager:latest
    ports:
      - "80:80"
      - "443:443"
      - "81:81" # painel de administração
    volumes:
      - proxy_data:/data
      - proxy_letsencrypt:/etc/letsencrypt
    networks:
      - internal_net
    restart: unless-stopped

volumes:
  pgdata:
  waha_sessions:
  waha_media:
  n8n_data:
  proxy_data:
  proxy_letsencrypt:

networks:
  internal_net:
    driver: bridge
